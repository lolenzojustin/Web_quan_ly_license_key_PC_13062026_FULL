# Backend URL for API calls. In local dev, it points directly to FastAPI.
# In same-domain production deployments, leave this unset so the browser origin
# is used automatically. Set it only if the API runs on a different domain.
NEXT_PUBLIC_API_URL=http://localhost:8000
